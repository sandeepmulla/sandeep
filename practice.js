let login = document.getElementById("login-sub");
console.log(login);

login.onclick = () =>{
    let form=document.getElementById("form-1")
    console.log(form);

    let display = form.classlist.toggle{
        if (display) {
            form.style.display = "block"

        }
       
    }
}